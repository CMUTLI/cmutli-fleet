# Keep SOPS configuration at the repository root. SOPS searches here when
# editing files below `secrets/`.
#
# Add one rule per host after verifying that host's SSH ed25519 fingerprint.
# Never commit a plaintext secret file.
creation_rules: [ ]

# Example:
# - path_regex: ^secrets/syllabus-registry-01\.yaml$
#   age: age1replace-this-with-the-host-key-recipient
